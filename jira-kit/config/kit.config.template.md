# Jira Kit Config — User / Device Level
<!--
  This file lives at ~/.claude/jira-kit.config.md
  Run /jira-setup to configure interactively.

  Authentication is handled by the Atlassian MCP via OAuth — no API token here.
  Run: /mcp authenticate atlassian
-->

## Jira
```
JIRA_BASE_URL=                            # e.g. https://mycompany.atlassian.net — used for building ticket URLs
SPRINT_DURATION_DAYS=14                   # sprint length in days (default: 2 weeks)
```
