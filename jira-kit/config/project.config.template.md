# Jira Kit Config — Project Level
<!--
  This file lives at .claude/jira.config.md inside each repository.
  Commit it to version control — it documents the Jira coordinates for the whole team.

  Run /jira-setup --project to generate interactively.
-->

## Project Jira Identity
```
JIRA_PROJECT_KEY=                         # e.g. ORD or CUST
JIRA_BOARD_ID=                            # numeric board ID (optional — used by standup for sprint data)
```
